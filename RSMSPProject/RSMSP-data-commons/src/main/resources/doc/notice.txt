开发备忘录：
1.在本项目中，实体类不再进行关联，属性只用基本数据类型表示。
分析：
由于本次项目采用Mybatis Plus作为数据持久层开发工具，因此只能进行单表查询。
因此，我们不需要像lscs中那样对实体类和视图都进行关联（即多表联查时，需要在java中表示
表间的关联关系-一对多或者多对多，需要将关联表作为一个属性添加在实体类中）。
这不仅表示过去的private List<Role> roleList这类表示多的属性关系不再需要（删除），也表示
类似于private Role role的这类表示一的属性关系也不再需要（转换为String进行表示）。